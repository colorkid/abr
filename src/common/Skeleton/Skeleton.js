import React from 'react';

const styles = {
    backgroundColor: '#d9dcde',
    height: '100%',
};

const Skeleton = () => {
    return (
        <div style={styles} />
    );
}

export default Skeleton
